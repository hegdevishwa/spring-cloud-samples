spring:
  config:
    name: configserver
  cloud:
    config:
      name: configserver
      server:
        git:
          uri: https://github.com/hegdevishwa/spring-cloud-samples.git
          basedir: target/config
server:
  port: 8000
logging:
  level: DEBUG